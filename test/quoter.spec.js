import {strictEqual as equal} from 'assert';
import {QuoteDesc} from '../models';

suite('Quoter', () => {
  test.skip('should get random quote', () => {
	});

  test.skip('should get random quote by author', () => {
	});

  test.skip('should get random quote by category', () => {
	});
});
